'use client'
import AppShell from '@/components/AppShell'
export default function CalendarPage() {
  return <AppShell><div style={{padding:'32px',color:'#5A5A56',fontSize:'13px',textAlign:'center'}}>📅 Calendar — coming in next session</div></AppShell>
}
